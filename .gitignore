# Credentials — jangan di-push ke repository
config/credentials.json

# Data Excel — berbeda tiap PC
data.xlsx

# Sesi browser (Sabrina login + WhatsApp) — berisi cookies/login data, beda tiap PC
session/

# Archive / distribusi
*.rar
*.zip

# Python cache
__pycache__/
*.pyc

# Virtual environment
.venv/

# Test cache
.pytest_cache/

# ChromeDriver cache (webdriver-manager)
.wdm/

# Error log runtime
error.log

# Lock file sementara saat multi-instance write ke Excel
data.xlsx.lock

# Dev scripts — tidak perlu di repo
bump_version.py
push.bat
